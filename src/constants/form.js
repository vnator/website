const FORM = Object.freeze({
  EMAIL: 'email',
  SUBJECT: 'subject',
  NAME: 'name',
  MESSAGE: 'message',
});

const TYPE = Object.freeze({
  TEXT: 'text',
  EMAIL: 'email',
});

const HINT = Object.freeze({
  MIN: 'MIN',
  MAX: 'MAX',
  EMAIL: 'EMAIL',
  REQUIRED: 'REQUIRED',
});

export { FORM, TYPE, HINT };
